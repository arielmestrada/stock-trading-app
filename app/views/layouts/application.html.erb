<!DOCTYPE html>
<html>
  <head>
    <title>StockTradingApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>
    
  <% if user_signed_in? %>      
      <h1>Header</h1>
      <%= link_to 'Home', root_path%><br/>
      <%= link_to 'Market Listings', listings_path %><br/>
      
      <% if can? :manage, User %>
        <%= link_to 'Create trader account', new_trader_path %><br/>
        <%= link_to 'Transactions', transactions_path %>
      <% else %>
        <%= link_to 'Edit Profile', edit_user_registration_path %><br/>
        <%= link_to 'My Portfolio', portfolio_path %><br/>
        <%= link_to 'Transaction History', transactions_path %>
      <% end %>
      
      <%= button_to "Log out", destroy_user_session_path, :method => :delete %><br/>
    <% end %>
    <%= yield %>
  </body>
</html>
